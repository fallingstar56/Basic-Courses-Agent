build(v2).py可以自动读取...\\processed的所有子文夹下的combined\_result\_examples.json文件并在Basic-Courses-Agent文件夹下创建对应的faiss文件，构建例题对应的RAG。

我觉的之后可以再另外创建一个知识库RAG。



build(v2).py 是采用最暴力的L2 method，很精确，之后的知识点RAG不能采用这种方式

